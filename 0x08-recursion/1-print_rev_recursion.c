#include "main.h"

/**
 * _puts_recursion - prints a string
 * @s: pointer block  of memory
 * Return: void
 */
 
void _print_rev_recursion(char *s)
{
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}

	_putchar (*s);
	_puts_recu)}
